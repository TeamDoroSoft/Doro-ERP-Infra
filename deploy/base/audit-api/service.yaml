apiVersion: v1
kind: Service
metadata:
  name: audit-api
  labels:
    app.kubernetes.io/name: audit-api
    app.kubernetes.io/part-of: doro-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: audit-api
  ports:
    - name: http
      port: 8085
      targetPort: http
      protocol: TCP
      appProtocol: http
