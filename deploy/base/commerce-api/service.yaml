apiVersion: v1
kind: Service
metadata:
  name: commerce-api
  labels:
    app.kubernetes.io/name: commerce-api
    app.kubernetes.io/part-of: doro-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: commerce-api
  ports:
    - name: http
      port: 8082
      targetPort: http
      protocol: TCP
      appProtocol: http
