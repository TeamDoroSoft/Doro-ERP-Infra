apiVersion: v1
kind: Service
metadata:
  name: store-access-api
  labels:
    app.kubernetes.io/name: store-access-api
    app.kubernetes.io/part-of: doro-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: store-access-api
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
      appProtocol: http
