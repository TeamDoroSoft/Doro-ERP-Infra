apiVersion: v1
kind: Service
metadata:
  name: edge-api
  labels:
    app.kubernetes.io/name: edge-api
    app.kubernetes.io/part-of: doro-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: edge-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
      appProtocol: http
